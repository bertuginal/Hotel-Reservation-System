@model Hotel_Reservation_System.Models.Admin

@{
    ViewBag.Title = "Login Admin";
}

<link href="~/Content/css-folder/account/admin/loginadminn.css" rel="stylesheet" type="text/css" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

@using (Html.BeginForm("LoginAdmin", "Account", FormMethod.Post, new { @class = "form-horizontal", @role = "form" }))
{
    @Html.AntiForgeryToken()

    <div class="login-page">
        <div class="form-horizontal">

            <h5>Admin</h5>
            <h2>Welcome back</h2>
            <h4>Please enter your e-mail and password</h4>
            <br />

            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

            <div class="form-group">
                @Html.LabelFor(m => m.Email, new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.Label("Password*", new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    <div class="input-group password-wrapper">
                        <input type="password" id="passwordInput" name="password" class="form-control" />
                        <i class="fa-solid fa-eye-slash password-icon" id="togglePasswordIcon" onclick="togglePasswordVisibility()"></i>
                    </div>
                </div>
            </div>

            <div class="form-group ">
                <div class="forgot-remember">
                    <div class="col-md-6">
                        <p class="remember-me"><input type="checkbox" /> Remember Me</p>
                    </div>
                    <div class="col-md-6">
                        <a href="#" class="forgot-password">Forgot password</a>
                    </div>
                </div>
            </div>


            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Sign in" class="btn btn-dark" />
                </div>
            </div>

            <div class="separator">
                <hr />
                <span>or sign up with</span>
                <hr />
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <button class="btn btn-google">
                        <img src="~/Content/images/icon/google-icon.png" alt="Google Icon" class="google-icon" />
                        Sign in with Google
                    </button>
                </div>
            </div>

        </div>
    </div>
}


<script>
    function togglePasswordVisibility() {
        var passwordInput = document.getElementById("passwordInput");
        var toggleBtnIcon = document.getElementById("togglePasswordIcon");

        if (passwordInput.type === "password") {
            passwordInput.type = "text";
            toggleBtnIcon.classList.remove("fa-eye-slash");
            toggleBtnIcon.classList.add("fa-eye");
        } else {
            passwordInput.type = "password";
            toggleBtnIcon.classList.remove("fa-eye");
            toggleBtnIcon.classList.add("fa-eye-slash");
        }
    }
</script>